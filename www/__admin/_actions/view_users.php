<?php	$man = DBHelper::Query("SELECT * FROM managers");    $managers = array();    foreach($man as $m){        $managers[ $m['id'] ] = $m['user'];    }        $time = time();	$users = DBHelper::Query("SELECT * FROM users ORDER BY expiration > {$time} DESC, zone ASC, expiration DESC, name ASC", $error);        //var_dump($error);	$k = 1;        // Includes the html document that displays the content    require AROOT . "/_actions/user_list.html";